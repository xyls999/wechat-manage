# GitHub + Render 部署指南

## 第一步：上传到GitHub

### 1. 初始化Git仓库
```bash
cd E:\fastapi
git init
git add app/ main.py requirements.txt .env .gitignore README.md render.yaml Procfile
git commit -m "Initial commit: FastAPI Excel Processor"
```

### 2. 创建GitHub仓库
1. 访问 https://github.com/new
2. 创建新仓库（例如：fastapi-excel-processor）
3. **不要**勾选"Initialize with README"

### 3. 推送到GitHub
```bash
git remote add origin https://github.com/你的用户名/fastapi-excel-processor.git
git branch -M main
git push -u origin main
```

## 第二步：Render部署

### 1. 登录Render
访问 https://render.com 并登录

### 2. 创建Web Service
1. 点击 "New +" → "Web Service"
2. 选择 "Connect a repository"
3. 授权GitHub并选择你的仓库
4. Render会自动检测到 `render.yaml` 配置

### 3. 配置设置
- **Name**: fastapi-excel-processor（或自定义）
- **Environment**: Python 3
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`
- **Plan**: Free（免费套餐）

### 4. 添加环境变量
在 "Environment" 标签页添加：

| 键 | 值 |
|---|---|
| SECRET_KEY | 点击"Generate"生成随机密钥 |
| DATABASE_URL | `sqlite+aiosqlite:///./app.db` |
| UPLOAD_DIR | `./uploads` |
| CORS_ORIGINS | `["*"]` |

### 5. 部署
点击 "Create Web Service"，Render会自动：
- 克隆你的GitHub仓库
- 安装 requirements.txt 中的依赖
- 启动FastAPI应用

## 部署完成后

### 获取API地址
部署成功后，Render会提供一个URL，例如：
```
https://fastapi-excel-processor.onrender.com
```

### 测试API
访问：
- API文档: `https://你的域名.onrender.com/docs`
- 健康检查: `https://你的域名.onrender.com/health`

### 前端对接
将前端代码中的 `BASE_URL` 修改为：
```javascript
const BASE_URL = 'https://你的域名.onrender.com/api/v1'
```

## ⚠️ 重要提示

1. **数据持久化**: 免费版Render每次重启会清空文件，建议升级或使用云存储
2. **冷启动**: 免费服务15分钟无请求会休眠，首次访问会慢
3. **数据库**: 生产环境建议使用PostgreSQL而非SQLite
4. **文件存储**: 建议使用云存储（阿里云OSS/AWS S3）

## 🔄 更新部署

每次代码更新后：
```bash
git add .
git commit -m "更新说明"
git push
```
Render会自动重新部署！

## 📝 需要上传的核心文件

✅ 必须：
- app/ 目录（所有Python代码）
- main.py
- requirements.txt
- .gitignore
- README.md

✅ 推荐：
- render.yaml（自动配置）
- Procfile（启动命令）
- .env（环境变量模板，实际值在Render配置）

❌ 不要：
- venv/（虚拟环境）
- __pycache__/（缓存）
- *.db（数据库文件）
- uploads/（上传文件）
- test_*.py（测试文件）
